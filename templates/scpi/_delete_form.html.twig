<form method="post" action="{{ path('scpi_delete', {'id': scpi.id}) }}" onsubmit="return confirm('Etes vous sûre de vouloir supprimer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scpi.id) }}">
    <button class="btn btn-dark">Supprimer</button>
</form>