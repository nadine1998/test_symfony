<form method="post" action="{{ path('scpi_delete', {'id': scpi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scpi.id) }}">
    <button class="btn">Delete</button>
</form>
