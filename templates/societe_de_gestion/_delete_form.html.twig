<form method="post" action="{{ path('societe_de_gestion_delete', {'id': societe_de_gestion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ societe_de_gestion.id) }}">
    <button class="btn">Delete</button>
</form>
